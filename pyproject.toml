[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
log_cli = true
log_cli_level = "INFO"
markers = [
    "integration: requires live infrastructure (RabbitMQ, S3, Temporal, etc.)",
]
